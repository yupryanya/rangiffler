mutation LikePhoto($input: PhotoInput!) {
    photo(input: $input) {
        id
        description
        likes {
            total
            likes {
                user
            }
        }
    }
}